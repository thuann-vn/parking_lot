{
  "name": "parking-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./app.js ./input.txt",
    "test": "mocha",
    "run-docker": "docker build --no-cache -t parking-lot-app . && docker run -p 49160:8080 -d parking-lot-app"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "mocha": "^10.2.0",
    "morgan": "~1.9.1"
  }
}
